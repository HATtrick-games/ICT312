#pragma once

namespace Physics
{
	struct Constants
	{
		//static const Ogre::Vector3	GRAVITY;
		/*static const float			MATH_PI = 3.14159;

		static const float			MIN_Y = 0.0f;
		static const float			MAX_Y = 1000.0f;
		static const float			MIN_X = -1000.0f;
		static const float			MAX_X = 1000.0f;
		static const float			MIN_Z = -1000.0f;
		static const float			MAX_Z = 1000.0f;*/

		/*static float clamp(float value, float min, float max)
		{
			if(value >= max)
			{
				return max;
			}
			else if(value <= min)
			{
				return min;
			}
			else
			{
				return value;
			}
		}*/
	};
}